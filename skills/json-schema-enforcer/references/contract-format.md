# Contract Format

The lightweight contract format supports:
- `required_fields`: list of required top-level keys
- `field_types`: map of key to expected primitive type (`string`, `number`, `integer`, `boolean`, `object`, `array`)
- `allowed_values`: map of key to allowed literal values
- `forbidden_fields`: list of disallowed top-level keys
