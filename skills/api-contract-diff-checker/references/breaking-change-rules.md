# Breaking Change Rules

Breaking by default:
- Removed endpoint path
- Removed HTTP method under existing path
- Added required request field

Review required:
- Enum narrowing
- Type narrowing with migration strategy
