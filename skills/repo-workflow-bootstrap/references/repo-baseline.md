# Repo Baseline

Minimum baseline files:
- `README.md` with setup and test commands
- CI workflow (`.github/workflows/ci.yml` or equivalent)
- lockfile and package manager consistency
- script contract for lint/test/build
