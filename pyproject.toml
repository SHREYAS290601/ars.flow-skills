[project]
name = "skills-catalog"
version = "0.1.0"
description = "Source-of-truth skill catalog with validation and registry generation tooling"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
